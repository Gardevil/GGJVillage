using UnityEngine;
using System.Collections;

public class Amamama : MonoBehaviour {

    public string a;

	// Use this for initialization
	void Start () {
	//base.GetComponent<>
	}
	
	// Update is called once per frame
	void Update () {
	    if (Input.GetKey(KeyCode.LeftArrow))
        {
            //transform.position.x = 0;
        }
	}
}
